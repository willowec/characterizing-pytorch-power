# PyTorch CNN Energy Predictions

## Permissions

you may need to allow userspace reading of the power meter

        sudo chmod -R a+r /sys/class/powercap/intel-rapl/

## Setup environment

        python3 -m venv .venv
        source .venv/bin/activate

